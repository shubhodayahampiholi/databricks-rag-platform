resources:
  dashboards:
    bronze_backlog:
      display_name: "${var.team_name} Bronze Ingestion Backlog"
      file_path: ../src/bronze/bronze_backlog.lvdash.json
      warehouse_id: "${var.sql_warehouse_id}"